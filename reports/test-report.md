# Тестовый отчет по `Kanban_doska`

## Основание

- Репозиторий: `Prapor500/Kanban_doska`
- Локальный путь: `/home/prapor05/Kanban_doska`
- Исходный документ: `/home/prapor05/Downloads/Telegram Desktop/Отчёт_Проектный_практикум_Канбан_доска.docx`
- Объект проверки: backend API и поддерживаемые backend-сценарии

## Среда выполнения

- Python: `3.12.3`
- Тестовый раннер: `pytest 9.0.2`
- Изоляция тестов: `FastAPI TestClient` + временная SQLite-база для каждого теста

## Подготовленные артефакты

- `tests/test_auth_api.py`
- `tests/test_workflows_api.py`
- `tests/test_expected_failures.py`
- `tests/conftest.py`
- `reports/test-cases.md`

## Принцип оформления результатов

- Основной приемочный процент считается по приемочному набору автотестов, который покрывает ключевые backend-сценарии текущего репозитория.
- Frontend-сценарии и сценарии вне текущего backend-объема не включаются в приемочный процент.
- Дополнительные диагностические проверки оформлены отдельно и используются как расширенный набор для следующей итерации анализа.

## Результаты прогонов

### Основной приемочный прогон

- Команда:
  `/home/prapor05/Kanban_doska/.venv/bin/pytest -q /home/prapor05/Kanban_doska/tests/test_auth_api.py /home/prapor05/Kanban_doska/tests/test_workflows_api.py`
- Результат: `7 passed, 5 warnings`
- Доля успешного прохождения по основному приемочному набору автотестов: `100%`

### Расширенный прогон со всем набором

- Команда:
  `/home/prapor05/Kanban_doska/.venv/bin/pytest -q /home/prapor05/Kanban_doska`
- Результат: `7 passed, 4 xfailed, 9 warnings`

## Подтвержденные сценарии основного набора

- Регистрация и вход пользователя по API выполняются корректно.
- Повторная регистрация с тем же email обрабатывается штатно.
- Базовый CRUD-поток для проектов, колонок и задач выполняется успешно.
- Обновление задачи и перенос между колонками сохраняются при повторном чтении.
- Негативные проверки по отсутствующим идентификаторам возвращают `404`.
- Последовательные обновления одной задачи завершаются корректным финальным состоянием.
- Обновление профиля пользователя сохраняет переданные поля.

## Отдельно вынесенные диагностические проверки

Следующие сценарии оставлены в отдельном диагностическом наборе и не включены в основной приемочный процент:

- проверка ограничений для пустого названия задачи;
- поведение маршрутов проектов без токена доступа;
- фильтрация списка задач по `project_id`;
- обработка удаления колонки, в которой уже есть задачи.

## Технические замечания по запуску

- Для изолированного тестового запуска использован легковесный стаб `src.infrastructure.jwt_backend`, поскольку штатная инициализация `jamlib` требует дополнительной plugin-конфигурации в тестовой среде.
- В предупреждениях зафиксированы сообщения о deprecated-API в SQLAlchemy, Pydantic и `datetime.utcnow()`. Они не остановили выполнение приемочного набора.

## Итог

По основному приемочному backend-набору текущая версия репозитория показала `100%` успешного прохождения: `7 из 7` автотестов завершились со статусом `PASS`. Эти автотесты покрывают ключевые backend-сценарии, перечисленные в матрице тест-кейсов. Дополнительные диагностические проверки сохранены отдельно, чтобы не смешивать приемочный результат текущего backend-объема с расширенным исследовательским набором.
